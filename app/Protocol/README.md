Protocol layer
==============

This project currently has no standalone frame parser, response builder, or
CRC module. Add communication protocol code in this folder when those modules
are implemented.
