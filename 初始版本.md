# 2026 CIMC 工业嵌入式系统初赛代码笔记

> 版本：初赛初始版本  
> 更新日期：2026-08-13  
> 队伍编号：`2026834095`  
> 分析依据：当前 `app`、`BootLoader` 源码和《2026 年 CIMC 工业嵌入式系统开发初赛赛题》  
> 阅读目标：从自动评分角度说明每个得分点由哪些代码函数完成，以及函数之间如何调用。

## 1. 先从赛题理解工程

赛题的软件评测按 A 至 N 顺序连续执行，共 14 个模块、38 个评分项。代码也围绕这个顺序组织：

- `app` 负责 A-M：通信、查询、参数、采样、持久化、告警、睡眠以及发起升级。
- `BootLoader` 主要负责 N：接收固件、校验、搬运、回滚和跳转。
- `Driver` 是硬件层，提供 USART、ADC、DAC、RTC、OLED、SPI Flash 等底层能力。
- `Protocol` 是协议层，负责 ASCII 帧解析、CRC、组帧和命令分发。
- `Function` 是业务层，保存设备参数并完成采样、上报、告警、睡眠和升级决策。

赛题规定协议帧先按十六进制结构组装，再以 ASCII 字符串发送。例如二进制帧头 `0xA5B6` 实际发送字符 `A5B6`。代码中的 `proto_parse()` 和 `proto_build()` 正是这一要求的核心实现。

## 2. APP 所有评分项共用的调用链

除 BootLoader 升级阶段外，上位机命令都经过同一条链路：

```text
上位机通过 USART1/RS485 发送 ASCII 帧
  -> USART1_IRQHandler() 检测串口 IDLE
  -> DMA 接收长度写入 rs_rx_len，并置 rs_rx_flag
  -> scheduler_run() 每 5 ms 调用 uart_task()
  -> uart_task() 调用 proto_dispatch()
  -> proto_parse() 校验 ASCII、地址、长度、CRC
  -> proto_dispatch() 根据 frame_type 和 cmd_word 路由
  -> sys_* 业务函数读取或修改状态
  -> proto_build_ack()/ok()/err()/hb() 组帧
  -> rs_usart_send() 控制 RS485 DE 并发送 ASCII 应答
```

关键入口如下：

| 层次 | 函数 | 文件位置 | 作用 |
|---|---|---|---|
| 中断接收 | `USART1_IRQHandler()` | `app/USER/src/gd32f4xx_it.c:191` | USART1 IDLE 中断切出 DMA 数据，设置收包标志 |
| 任务调度 | `scheduler_run()` | `app/Function/APP/scheduler.c:43` | 周期调用 `uart_task()` 等任务 |
| 应用通信 | `uart_task()` | `app/Function/APP/usart_app.c:66` | 取包、分发、发应答、周期上报、告警和延迟动作 |
| 命令分发 | `proto_dispatch()` | `app/Protocol/proto_dispatch.c:23` | A-M 评分命令的主要路由函数 |
| 帧解析 | `proto_parse()` | `app/Protocol/proto_frame.c:66` | ASCII 转二进制并校验帧格式和 CRC16 |
| 帧构造 | `proto_build()` | `app/Protocol/proto_frame.c:145` | 二进制字段组帧后转为 ASCII 字符串 |
| RS485 发送 | `rs_usart_send()` | `app/Function/APP/usart_app.c:49` | 控制 DE，等待 TBE/TC，完成半双工发送 |

因此某个评分项失败时，应按“收包中断 → 协议解析 → 命令分支 → 业务函数 → 组帧发送”的顺序定位，而不是只检查一个命令分支。

## 3. A 模块：广播、重启和心跳，共 4 分

### A-01 广播寻找设备，1 分

上位机发送心跳类型 `0x05`、命令字 `0xFFFF`、设备地址 `0xFFFF`。

调用链：

```text
proto_dispatch()
  -> 识别 PROTO_TYPE_HB + CMD_HB_PC_SCAN
  -> sys_device_get_id()
  -> proto_build_hb(device_id)
  -> proto_build()
  -> rs_usart_send()
```

得分依赖：

- `proto_dispatch()`：`app/Protocol/proto_dispatch.c:67`，识别广播扫描。
- `sys_device_get_id()`：`app/Function/sysFunction/sys_device.c:10`，读取当前设备 ID。
- `proto_build_hb()`：`app/Protocol/proto_frame.c:236`，构造 `05/8888` 心跳帧。
- `proto_parse()` 必须接受广播地址，地址过滤逻辑不能提前丢弃广播。

### A-02 发送重启指令，2 分

调用链：

```text
proto_dispatch(CMD_REBOOT)
  -> proto_build_ok()
  -> sys_device_request_reboot()
  -> uart_task() 先 rs_usart_send(OK)
  -> sys_device_reboot_if_pending()
  -> NVIC_SystemReset()
```

得分关键是“先应答、后复位”。`proto_dispatch()` 只设置延迟标志，不直接复位，避免 OK 帧尚未发完设备就重启。

- 命令分支：`app/Protocol/proto_dispatch.c:72`。
- 设置延迟复位：`sys_device_request_reboot()`，`sys_device.c:41`。
- 执行复位：`sys_device_reboot_if_pending()`，`sys_device.c:46`。
- 延迟动作入口：`uart_task()` 在发送应答后调用复位函数。

### A-03 重启后等待心跳，1 分

`s_startup_hb_pend` 初始为 1。`uart_task()` 第一次运行时读取该标志，构造并发送一次 `05/8888` 心跳，然后清除标志。

- `sys_device_startup_hb_pending()`：`sys_device.c:57`。
- `sys_device_clear_startup_hb()`：`sys_device.c:62`。
- 心跳发送逻辑：`uart_task()`，`usart_app.c:69` 附近。
- 心跳设备 ID 来自 `sys_storage_init()` 恢复后的当前 ID，因此应与重启前一致。

## 4. B 模块：基础查询，共 13 分

### B-01 查询固件版本，1 分

```text
proto_dispatch(CMD_QUERY_FW_VER)
  -> sys_device_get_fw_version()
  -> proto_build_ack(payload=02 00 01 00)
```

- 命令分支：`proto_dispatch.c:111` 附近。
- 版本读取：`sys_device_get_fw_version()`，`sys_device.c:21`。
- 版本宏：`app/Function/sysFunction/sys_device.h`，当前为 `2.0.1.0`。

### B-02 查询设备时间，2 分

```text
proto_dispatch(CMD_GET_TIME)
  -> sys_time_get_utc_seconds()
  -> 将 uint32_t 拆成 4 字节大端
  -> proto_build_ack()
```

- 分发分支：`proto_dispatch.c:121` 附近。
- RTC BCD 转 UTC：`sys_time_get_utc_seconds()`，`sys_time.c:24`。
- 硬件依赖：`bsp_rtc_init()` 在 `main()` 中先完成 RTC 初始化。

### B-03 查询波特率，1 分

- `proto_dispatch()` 的 `CMD_GET_BAUDRATE` 分支调用 `sys_device_get_baudrate_code()`。
- `sys_device_get_baudrate_code()`：`sys_device.c:30`。
- 返回映射码，不直接返回整数波特率：`11=4800`、`12=9600`、`13=19200`、`14=115200`。
- 默认宏 `SYS_DEVICE_BAUD_CODE_DEFAULT` 为 `0x13`。

### B-04 查询 CH0，3 分

```text
ADC0_CH10/PC0
  -> DMA 循环更新 adc_value[0]
  -> sys_sample_get_ch0()
  -> adc_raw / 4095 * 3.3 * CH0 ratio
  -> proto_pack_float_be()
  -> proto_build_ack()
```

- ADC 底层初始化：`bsp_adc_init()`，`mcu_cimc_gd32f470vet6.c:520` 附近。
- 业务换算：`sys_sample_get_ch0()`，`sys_sample.c:17`。
- 变比读取：`sys_param_get_ch0_ratio()`，`sys_param.c:9`。
- 浮点大端编码：`proto_pack_float_be()`，`proto_frame.c:246` 附近。
- 分发分支：`CMD_READ_CH0`，`proto_dispatch.c:142` 附近。

### B-05 查询 CH1，3 分

```text
PA4 DAC 输出，经跳线送到 PC1
  -> bsp_adc1_read_ch1() 触发 ADC1_CH11 单次转换
  -> sys_sample_get_ch1()
  -> 电压换算 * CH1 ratio
  -> proto_pack_float_be()
  -> proto_build_ack()
```

- ADC1 初始化：`bsp_adc1_init()`，由 `main()` 调用。
- ADC1 读取：`bsp_adc1_read_ch1()`，BSP 文件 `mcu_cimc_gd32f470vet6.c:680` 附近。
- 业务换算：`sys_sample_get_ch1()`，`sys_sample.c:22`。
- 分发分支：`CMD_READ_CH1`，`proto_dispatch.c:151` 附近。
- 硬件得分依赖：必须正确连接 PA4-PC1，仅有代码无法形成 DAC 回读。

### B-06 查询 CH0/CH1 阈值，3 分

- `CMD_READ_THRESH_ALL` 分支分别调用 `sys_param_get_ch0_thresh()` 和 `sys_param_get_ch1_thresh()`。
- 两个 float 经 `proto_pack_float_be()` 形成 8 B payload。
- 参数函数：`sys_param.c:15`、`sys_param.c:18`。
- 默认阈值：`SYS_PARAM_THRESH_DEFAULT = 21.59f`。

### 额外功能：CH2/PT100

CH2 不在 A-N 软件自动评分表中，但属于赛题硬件测评内容：

```text
CMD_READ_CH2_PT100
  -> sys_pt100_get_temp()
  -> GD30AD3344 通道采样
  -> 电阻校准和 PT100 温度换算
  -> proto_pack_float_be()
```

核心函数为 `sys_pt100_get_temp()`，`app/Function/sysFunction/sys_pt100.c:100`。

## 5. C 模块：设置时间，共 2 分

### C-01 设置时间并回读，2 分

```text
proto_dispatch(CMD_SET_TIME)
  -> 检查 payload_len == 4
  -> 大端 4 B 合成 UTC uint32_t
  -> sys_time_set_utc_seconds()
  -> UTC 日期拆分并写 RTC BCD 寄存器
  -> proto_build_ok()
```

- 命令分支：`proto_dispatch.c:132` 附近。
- 时间设置：`sys_time_set_utc_seconds()`，`sys_time.c:84`。
- 回读仍依赖 B-02 的 `sys_time_get_utc_seconds()`。
- 长度错误由 `proto_build_err(..., CMD_ERR_WORD)` 返回统一错误帧。

## 6. D 模块：变比即时生效，共 6 分

### D-00 DAC 预备，不计分

这是上位机为后续 CH1 测试准备 DAC 输出，不是独立得分项。代码依赖 G-01 的 `CMD_SET_DAC -> sys_dac_set_raw()` 链路。

### D-01 CH0 变比即时生效，3 分

```text
proto_dispatch(CMD_SET_RATIO_CH0)
  -> proto_unpack_float_be()
  -> sys_param_set_ch0_ratio()
  -> sys_storage_save()

后续 CMD_READ_CH0
  -> sys_sample_get_ch0()
  -> 原始电压 * 新 ratio
```

- 设置分支：`proto_dispatch.c:181` 附近。
- 大端浮点解码：`proto_unpack_float_be()`。
- 参数更新：`sys_param_set_ch0_ratio()`，`sys_param.c:10`。
- 即时生效点：`sys_sample_get_ch0()` 每次读取当前 ratio，而非缓存旧结果。

### D-02 CH1 变比即时生效，3 分

调用链与 D-01 相同，替换为：

- `CMD_SET_RATIO_CH1`。
- `sys_param_set_ch1_ratio()`，`sys_param.c:13`。
- `sys_sample_get_ch1()`，`sys_sample.c:22`。

## 7. E 模块：阈值设置与回读，共 6 分

### E-01 CH0 阈值设置与回读，3 分

- 写入：`CMD_WRITE_THRESH_CH0 -> proto_unpack_float_be() -> sys_param_set_ch0_thresh()`。
- 单独读取：`CMD_READ_THRESH_CH0 -> sys_param_get_ch0_thresh()`。
- 批量读取：`CMD_READ_THRESH_ALL` 从同一个 getter 取 CH0，因此两处结果一致。
- setter 位于 `sys_param.c:16`，并调用 `sys_storage_save()`。

### E-02 CH1 阈值设置与回读，3 分

- 写入：`CMD_WRITE_THRESH_CH1 -> sys_param_set_ch1_thresh()`。
- 单独读取：`CMD_READ_THRESH_CH1 -> sys_param_get_ch1_thresh()`。
- 批量读取：`CMD_READ_THRESH_ALL` 复用同一 getter。
- setter 位于 `sys_param.c:19`。

## 8. F 模块：重启后参数持久化，共 8 分

F-01 至 F-04 共用一套持久化链路：

```text
sys_param_set_*(new_value)
  -> sys_storage_save()
  -> 收集当前全部业务参数
  -> spi_flash_sector_erase(0x000000)
  -> spi_flash_buffer_write(SysStorageBlob)

设备重启
  -> main()
  -> bsp_gd25qxx_init()
  -> sys_storage_init()
  -> 校验 magic/version/checksum
  -> s_loading=1
  -> 调用各 setter 恢复参数但禁止重复写 Flash
```

| 得分点 | 持久字段 | 写入函数 | 重启加载后的验证函数 |
|---|---|---|---|
| F-01，2 分 | CH0 ratio | `sys_param_set_ch0_ratio()` | `sys_sample_get_ch0()` |
| F-02，2 分 | CH1 ratio | `sys_param_set_ch1_ratio()` | `sys_sample_get_ch1()` |
| F-03，2 分 | CH0 threshold | `sys_param_set_ch0_thresh()` | `sys_param_get_ch0_thresh()` |
| F-04，2 分 | CH1 threshold | `sys_param_set_ch1_thresh()` | `sys_param_get_ch1_thresh()` |

核心依赖：

- 保存函数：`sys_storage_save()`，`app/Function/sysFunction/sys_storage.c:53`。
- 加载函数：`sys_storage_init()`，`sys_storage.c:79`。
- 外部 Flash 驱动：`spi_flash_sector_erase()`、`spi_flash_buffer_write()`、`spi_flash_buffer_read()`。
- 初始化顺序：`main()` 必须先初始化 GD25Qxx，再调用 `sys_storage_init()`。

## 9. G 模块：DAC 联动，共 2 分

### G-01 设置 DAC 并验证 CH1 回读，2 分

```text
proto_dispatch(CMD_SET_DAC)
  -> 检查 payload_len == 2
  -> 合成 12 位 raw
  -> sys_dac_set_raw(raw)
  -> dac_data_set(DAC0, DAC_OUT0, ..., raw)
  -> sys_storage_save()
  -> proto_build_ok()

随后查询 CH1
  -> bsp_adc1_read_ch1()
  -> sys_sample_get_ch1()
```

- 命令分支：`proto_dispatch.c:238` 附近。
- DAC 封装：`sys_dac_set_raw()`，`sys_dac.c:11`。
- 底层输出：GD32 标准库 `dac_data_set()`。
- 回读依赖 B-05 调用链和 PA4-PC1 跳线。
- 当前 `sys_dac_set_raw()` 将超范围值限制到 12 位范围，并把 DAC 值写入持久化 blob。

## 10. H 模块：自动上报，共 11 分并含扣分项

### H-01 自动上报完整测试，10 分

包含设置间隔、启动、首帧和周期发送四部分：

```text
CMD_SET_REPORT_INTV
  -> sys_report_set_interval_code()
  -> code_to_ms(01/02/03)
  -> sys_storage_save()

CMD_AUTO_REPORT_ON
  -> sys_report_start(now)
  -> sys_time_get_utc_seconds()
  -> sys_sample_get_ch0()/get_ch1()
  -> proto_build_auto_report()
  -> 返回首帧 12 B 数据

uart_task() 周期执行
  -> sys_report_should_send(now)
  -> proto_build_auto_report()
  -> rs_usart_send()
  -> sys_report_mark_sent(now)
```

- 状态模块：`app/Function/sysFunction/sys_report.c`。
- 启动函数：`sys_report_start()`，`sys_report.c:36`。
- 周期判断：`sys_report_should_send()`，`sys_report.c:49`。
- 上报组帧：`proto_build_auto_report()`，`proto_frame.c:268`。
- 周期发送入口：`uart_task()` 自动上报代码段。
- 12 B 内容：UTC 4 B + CH0 float 4 B + CH1 float 4 B，均为大端。

### H-02 上报期间不响应其他命令，扣分项

`proto_dispatch()` 在完成地址校验后先检查 `sys_report_is_enabled()`。当已启用且当前帧不是 `CMD_AUTO_REPORT_OFF` 时直接返回 0，不构造任何应答。

- 守卫代码：`app/Protocol/proto_dispatch.c:51`。
- 依赖函数：`sys_report_is_enabled()`，`sys_report.c:47`。
- 该守卫也覆盖解析错误应答，防止上报期间混入额外帧。

### H-03 停止自动上报，1 分

- `CMD_AUTO_REPORT_OFF -> sys_report_stop() -> proto_build_ok()`。
- `sys_report_stop()` 将 `s_enabled` 清零，之后 `sys_report_should_send()` 始终返回 0。
- 分发分支：`proto_dispatch.c:271`。

## 11. I 模块：告警，共 7 分

### I-01 开启主动告警，1 分

- `CMD_ALARM_REPORT_EN -> sys_alarm_set_mode(0x01) -> sys_storage_save() -> proto_build_ok()`。
- `sys_alarm_set_mode()`：`app/Function/sysFunction/sys_alarm.c:187`。
- 仅接受 `01=主动`、`02=被动`，非法模式返回错误应答。

### I-02 触发并验证主动告警，2 分

```text
uart_task() 每约 5 ms
  -> sys_alarm_tick()
  -> sys_sample_get_ch0()/get_ch1()
  -> sys_param_get_ch0_thresh()/get_ch1_thresh()
  -> check_channel()
  -> 检测“未超限 -> 超限”上升沿
  -> append_record() 写外部 Flash
  -> emit_active_alarm()
  -> sys_alarm_format_record()
  -> rs_usart_send(纯 ASCII)
```

- 周期检查：`sys_alarm_tick()`，`sys_alarm.c:202`。
- 单通道判定：`check_channel()`，`sys_alarm.c:157`。
- Flash 追加：`append_record()`，`sys_alarm.c:70`。
- 主动发送：`emit_active_alarm()`，`sys_alarm.c:141`。
- 格式化：`sys_alarm_format_record()`，`sys_alarm.c:119`。

### I-03 关闭主动告警后再触发，2 分

上位机将模式设为 `0x02`。`check_channel()` 仍执行 `append_record()`，但只有 `s_mode == SYS_ALARM_MODE_ACTIVE` 才调用 `emit_active_alarm()`，因此实现“有记录、无主动上报”。

### I-04 清除告警记录，2 分

该评分过程依赖查询和清除两条命令：

- 查询 `0x0602`：`sys_alarm_count()` 获取数量，`sys_alarm_get_record()` 倒序读取最近 10 条，逐行调用 `rs_usart_send()`；无记录直接发送 `empty\n`。
- 清除 `0x0603`：`sys_alarm_clear_all()` 擦除外部 Flash `0x001000` 告警扇区，再返回 OK。
- 查询分支：`proto_dispatch.c:296`。
- 清除分支：`proto_dispatch.c:316`。
- 清除函数：`sys_alarm_clear_all()`，`sys_alarm.c:228`。

## 12. J 模块：睡眠后自动唤醒，共 5 分

### J-01 深度睡眠和 RTC 唤醒，5 分

```text
proto_dispatch(CMD_SLEEP)
  -> proto_build_ok()
  -> sys_sleep_request(10)
  -> uart_task() 先发送 OK
  -> sys_sleep_execute_if_pending()
  -> configure_rtc_alarm(10)
  -> bsp_enter_deepsleep()
  -> RTC Alarm0/EXTI17 唤醒
  -> 发送 "instrument wakeup"
```

- 命令分支：`proto_dispatch.c:277`。
- 请求函数：`sys_sleep_request()`，`sys_sleep.c:23`。
- RTC 闹钟配置：`configure_rtc_alarm()`，`sys_sleep.c:51`。
- 实际睡眠：`sys_sleep_execute_if_pending()`，`sys_sleep.c:80`。
- BSP 低功耗入口：`bsp_enter_deepsleep()`，BSP 文件约 `:218`。
- 中断处理：`RTC_Alarm_IRQHandler()`，`sys_sleep.c:104`。

和重启一样，得分关键是延迟执行：必须先发完 OK，再进入睡眠。

## 13. K 模块：异常帧处理，共 6 分

### K-01 CRC 错误帧，2 分

```text
proto_parse()
  -> CRC-16-Modbus 计算结果不匹配
  -> 返回 PROTO_ERR_CRC
proto_dispatch()
  -> proto_try_get_device_id()
  -> 地址为本机或广播
  -> proto_build_err(my_id, CMD_ERR_WORD)
```

- CRC 校验：`proto_parse()`，`proto_frame.c:66`。
- 错误分支：`proto_dispatch.c:29` 附近。
- 错误命令字：`CMD_ERR_WORD = 0xEEEE`。

### K-02 长度错误帧，2 分

- `proto_parse()` 比较 `bin_len` 与 `13 + payload_len`，不一致返回 `PROTO_ERR_LEN_FIELD`。
- `proto_dispatch()` 与 K-01 共用错误处理，返回 `FF/EEEE`。
- 对已正确解析但具体命令 payload 长度错误的情况，各命令分支也检查 `rx.payload_len` 并构造错误帧。

### K-03 非法命令字，2 分

- 合法地址、合法帧类型但未匹配任何已实现命令时，执行 `proto_dispatch()` 末尾兜底。
- 先通过 `my_printf()` 记录调试日志，再调用 `proto_build_err(my_id, CMD_ERR_WORD)`。
- 兜底代码：`proto_dispatch.c:363` 附近。
- 未知帧类型也在 `proto_dispatch.c:58` 附近返回相同错误帧。

## 14. L 模块：设备 ID，共 6 分

### L-01 修改设备 ID，3 分

```text
proto_dispatch(CMD_SET_DEV_ID)
  -> 检查 payload_len == 2
  -> 组合 new_id
  -> 0000 或 FFFF：静默返回 0
  -> sys_device_set_id(new_id)
  -> sys_storage_save()
  -> proto_build_ok(new_id, CMD_SET_DEV_ID)
```

- 分发分支：`proto_dispatch.c:351`。
- setter：`sys_device_set_id()`，`sys_device.c:15`。
- 注意应答帧设备 ID 字段使用新 ID，而不是旧 ID。
- 后续所有命令都经过 `rx.device_id != broadcast && rx.device_id != my_id` 地址守卫，旧 ID 将不再得到应答。

### L-02 重启后 ID 持久化，3 分

- 写入依赖 `sys_device_set_id() -> sys_storage_save()`。
- 重启依赖 A-02 的延迟复位链路。
- 启动时 `sys_storage_init()` 调用 `sys_device_set_id(blob.device_id)` 恢复 ID。
- 查询依赖 `CMD_GET_DEV_ID -> sys_device_get_id() -> proto_build_ack(2 B ID)`，分支位于 `proto_dispatch.c:339`。
- A-03 心跳也读取恢复后的 ID，因此持久化失败会同时影响 L-02 心跳 ID 校验。

## 15. M 模块：波特率，共 6 分

### M-01 修改为 115200 后通信，3 分

```text
proto_dispatch(CMD_SET_BAUDRATE)
  -> sys_baudrate_code_to_value() 校验映射码
  -> sys_device_set_baudrate_code()
  -> sys_storage_save()
  -> proto_build_ok()（仍用旧波特率发送）
  -> sys_device_request_reboot()
  -> uart_task() 发完 OK 后复位

main() 重新启动
  -> BSP 先按默认 19200 初始化 USART1
  -> sys_storage_init() 恢复 baud_code
  -> sys_baudrate_apply(baud_code)
  -> USART1 切换为 115200
  -> A-03 心跳按新波特率发出
```

- 分发分支：`proto_dispatch.c:323`。
- 映射校验：`sys_baudrate_code_to_value()`，`sys_baudrate.c:9`。
- 应用波特率：`sys_baudrate_apply()`，`sys_baudrate.c:20`。
- 复位前 `sys_device_reboot_if_pending()` 将 RS485 DE 拉低，避免复位毛刺污染总线。

### M-02 重启后仍保持 115200，3 分

- 持久化写入：`sys_device_set_baudrate_code() -> sys_storage_save()`。
- 持久化恢复：`sys_storage_init() -> sys_device_set_baudrate_code()`。
- APP 应用：`main() -> sys_baudrate_apply()`。
- BootLoader 应用：`BootLoader main() -> bl_apply_persistent_baud()`，确保后续 N 模块仍能用当前波特率通信。

## 16. N 模块：BootLoader OTA，共 18 分

N 模块跨 APP 和 BootLoader 两个工程，不能只看 `proto_dispatch()`。

### N-01 进入 BootLoader，3 分

APP 侧调用链：

```text
proto_dispatch(CMD_OTA_REQUEST/0x0501)
  -> proto_build_ok()
  -> ota_request_fast_upgrade_mark()
  -> sys_device_request_reboot()
  -> uart_task() 发送 OK
  -> ota_apply_fast_upgrade_mark_if_pending()
  -> 写内部 Flash 参数 update_flag=FAST_UPGRADE
  -> oled_show_bootloader_screen()
  -> NVIC_SystemReset()
```

BootLoader 侧调用链：

```text
BootLoader main()
  -> bl_apply_persistent_baud()
  -> OLED_Init() 显示队伍号和 Bootloader
  -> bootloader_run()
  -> 读取主/备参数
  -> 识别 BL_UPDATE_FLAG_FAST_UPGRADE
  -> bl_handle_fast_upgrade()
  -> bl_wait_prepare_with_countdown()
```

关键函数：

- APP 命令分支：`app/Protocol/proto_dispatch.c:81`。
- 设置待写标志：`ota_request_fast_upgrade_mark()`，`ota_uart.c:484`。
- 真正提交参数：`ota_apply_fast_upgrade_mark_if_pending()`，`ota_uart.c:492`。
- BootLoader 入口：`bootloader_run()`，`BootLoader/Function/BootLoader/bl_core.c:861`。
- 快速升级流程：`bl_handle_fast_upgrade()`，`bl_core.c:713`。
- 10 秒提示与等待：`bl_wait_prepare_with_countdown()`，`bl_core.c:475`。

得分条件中的“OK + 初始化信息”分别由 APP 的 `proto_build_ok()` 和 BootLoader 的倒计时打印完成。

### N-02 错误固件验证，5 分

```text
BootLoader 等到 CMD_OTA_PREPARE/0x0502
  -> bl_recv_bin_and_check_magic()
  -> bl_uart_read_byte_nb() 逐字节接收
  -> 分 256 B 块写 BL_DOWNLOAD_START_ADDR
  -> 100 ms 空闲判断接收结束
  -> 检查前 4 B 是否为 5A A5 C3 3C
  -> 错误：proto_build_err(req_id, CMD_OTA_PREPARE)
  -> bl_rs485_send()
  -> 重新擦预接收区域并等待下一次 0x0502
```

- 准备命令识别：`bl_wait_prepare_with_countdown()` 和 `bl_wait_prepare_silent()`，`bl_core.c:475`、`:540`。
- bin 接收和魔术字：`bl_recv_bin_and_check_magic()`，`bl_core.c:585`。
- Flash 擦写：`bl_flash_erase_pages()`、`bl_flash_program_bytes()`，`bl_core.c:71`、`:105`。
- 错误应答：`proto_build_err(..., CMD_OTA_PREPARE)`，调用处约 `bl_core.c:755`。
- BootLoader RS485 发送：`bl_rs485_send()`，`bl_core.c:452`。

错误包后不立即跳 APP，而是继续等待下一次 `0x0502`，这是 N-03 能继续下发正确固件的前提。

### N-03 正确固件升级并验证，10 分

第一阶段是接收正确包并等待执行命令：

```text
bl_recv_bin_and_check_magic()
  -> magic 正确
  -> proto_build_ok(CMD_OTA_PREPARE)
  -> bl_wait_execute_cmd(5000 ms)
  -> proto_parse() 识别 CMD_OTA_EXECUTE/0x0503
  -> proto_build_ok(CMD_OTA_EXECUTE)
  -> 计算下载包 CRC32
  -> update_flag=PENDING，写 app_size/app_crc32
  -> bl_commit_param()
  -> NVIC_SystemReset()
```

第二阶段是复位后的安装：

```text
bootloader_run()
  -> 识别 PENDING
  -> bl_is_download_package_valid()
  -> bl_crc32_flash() 对下载固件复核 CRC32
  -> bl_is_app_vector_valid(APP1)
  -> bl_backup_app1()
  -> bl_copy_download_to_app1()
  -> bl_crc32_flash(APP1) 再次校验
  -> 成功：update_flag=IDLE，记录 UPDATE_OK
  -> 失败：bl_restore_app1_from_backup() 回滚
  -> bl_commit_param_page()
  -> NVIC_SystemReset()
  -> bl_jump_to_app(APP1)
```

关键函数：

- 执行命令识别：`bl_wait_execute_cmd()`，`bl_core.c:654`。
- 参数提交：`bl_commit_param()`，`bl_core.c:250`。
- 包合法性：`bl_is_download_package_valid()`，`bl_core.c:387`。
- CRC32：`bl_crc32_calc()` 和 `bl_crc32_flash()`，`bl_crc32.c:4`、`bl_core.c:431`。
- 旧 APP 备份：`bl_backup_app1()`，`bl_core.c:367`。
- 下载区搬运：`bl_copy_download_to_app1()`，`bl_core.c:420`。
- 失败回滚：`bl_restore_app1_from_backup()`，`bl_core.c:380`。
- APP 跳转：`bl_jump_to_app()`，`bl_core.c:827`，负责关中断、设置 VTOR/MSP 并调用复位向量。

## 17. 非独立评分但影响得分的显示和指示灯

### OLED

- APP：`oled_task()` 每 10 ms 刷新，第一行显示 `2026834095`；自动上报时第二行 `AutoSample`，否则 `IDLE`。
- 进入 BootLoader 前：`oled_show_bootloader_screen()` 先显示 `Bootloader`。
- BootLoader：独立执行 `OLED_Init()` 和 `OLED_ShowStr()`，避免冷启动黑屏。

函数位置：`app/Function/APP/oled_app.c:44`、`:57`，以及 `BootLoader/USER/src/main.c:56`。

### LED

- `led_ctrl_task()` 每 500 ms 翻转 LED1，形成 1 秒完整闪烁周期。
- 自动上报启用时 LED2 常亮，否则熄灭。
- 函数位置：`app/Function/sysFunction/led_ctrl.c:19`。

## 18. 评分项依赖关系总结

以下模块不是彼此独立，前面一个基础问题会造成后续连续失分：

| 基础依赖 | 可能影响的评分项 |
|---|---|
| USART1 DMA/IDLE 收包、RS485 DE 发送 | A-N 几乎全部软件项 |
| `proto_parse()`、CRC16、`proto_build()` | A-M 所有协议帧项，以及 BootLoader 的 0502/0503 |
| `sys_storage_init/save()` 和 GD25Qxx | F、L、M，也影响设备 ID、波特率、告警模式和 DAC |
| CH0 ADC 与 CH1 ADC/DAC 回读 | B-04、B-05、D、G、H、I |
| RTC | B-02、C、H 时间戳、I 告警时间、J 唤醒 |
| 延迟动作顺序 | A-02、J-01、M-01、N-01，均要求先应答后动作 |
| BootLoader 参数区和 Flash 分区 | N-01、N-02、N-03 |
| 当前波特率跨 APP/BL 同步 | M-01、M-02、全部 N 项 |

## 19. 当前代码风险和赛前检查

1. **默认波特率**：外部 Flash 可能残留自动评测写入的 115200。正式测评开头上位机强制 19200，必须确认设备出厂状态实际为 19200。
2. **BootLoader 静默启动**：无升级请求时赛题要求 5 秒内不输出。当前正常跳转前存在 `my_printf("BL: jumping to app...")`。如果调试 USART 与 RS485 评测链路相同，应删除或隔离该输出。
3. **APP 地址**：APP 链接地址和 `SCB->VTOR` 必须同时为 `0x08011000`，否则中断会跳到 BootLoader 向量表。
4. **CH1 硬件连接**：PA4-PC1 跳线缺失会连带丢失 B-05、D-02、G-01、H-01 和 CH1 告警相关分数。
5. **OTA 时序**：官方切片为 256 B、间隔约 5 ms；BootLoader 以 100 ms 空闲判结束，必须用官方上位机连续验证。
6. **Flash 擦写阻塞**：当前是裸机协作调度，外部/内部 Flash 擦除期间其他任务不能运行。
7. **持久化掉电保护**：APP 业务参数采用单扇区擦后重写，比赛功能可用，但写入中掉电可能恢复默认值。
8. **实测流程**：先全量编译并下载 BootLoader，再编译下载 APP；移除 SWD 后冷启动，连续跑完整 A-N，不能只逐项手工验证。

## 20. 结论

当前代码已经为 A-N 各评分项建立了明确实现路径。APP 侧的中心是 `USART1_IRQHandler() -> uart_task() -> proto_dispatch() -> sys_* -> proto_build_*()`；BootLoader 侧的中心是 `bootloader_run() -> bl_handle_fast_upgrade() -> 接收/校验 -> PENDING 安装 -> bl_jump_to_app()`。

答辩或代码讲解时，应从上位机发送的命令字开始，依次说明协议解析、分发分支、业务函数、状态保存、应答组帧和硬件依赖。这样能直接证明每个评分点不是孤立代码，而是一条可以实际运行和验证的完整调用链。
