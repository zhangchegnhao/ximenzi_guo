# 2026 CIMC 工业嵌入式系统开发全国总决赛样题分析

> 文档性质：备赛分析，不是正式赛题答案  
> 分析对象：《2026 年 CIMC 工业嵌入式系统开发方向（试）全国总决赛 样题》  
> 编写日期：2026-08-13  
> 重要声明：当前文件是全国总决赛**样题**。正式比赛的通道定义、报文、寄存器表、参数、评分办法和现场追加任务均可能变化，本文只能用于建立通用技术底座，不能当作正式赛题逐字执行。

## 1. 样题传递出的核心变化

初赛重点是按照固定协议清单完成 A-N 自动评测；国赛样题把重点扩展到一个完整工业采集系统，要求队伍同时具备：

1. 模拟前端电路设计能力。
2. 嘉立创 EDA 原理图和 PCB 开发能力。
3. GD30AD3344 多通道 SPI ADC 驱动与标定能力。
4. TF 卡文件系统和连续数据存储能力。
5. 原有串口私有协议的现场适配能力。
6. 使用 FreeModbus 库开发 Modbus 从站的能力。
7. 在无网络环境下完成临时需求开发、调试和提交的能力。
8. 系统方案、测试方案、纸质/电子文档和现场答辩能力。

因此，国赛样题不是“在初赛代码上再加几个命令”，而是从协议功能题升级为“硬件采样板 + 嵌入式软件 + 工业通信 + 数据存储 + 工程文档”的综合任务。

## 2. 信息可信度分级

为避免把样题误当正式题，本文将信息分成三类。

### 2.1 样题明确写出的硬要求

- 使用统一实验开发平台，V1 或 V2 均可。
- 在同一块采样板上实现一路 4-20 mA 和两路 0-10 V 采样。
- 三路测量误差不大于 `+-1%`。
- 4-20 mA 通道具备断线检测。
- 采样 ADC 使用 GD30AD3344，并通过 SPI 与主控通信。
- 三路信号相互隔离，能够同时实现三路采集。
- 支持 TF 卡文件读写和多通道数据存储。
- 按给定串口报文完成数据交互。
- 支持静态参数、定制参数读写。
- 使用 FreeModbus 库完成 Modbus 从站。
- 使用 GD 固件库；使用非 GD 库总分扣 5 分。
- PCB 覆铜层必须写队伍编号，否则采样部分扣 20 分。
- 现场连接器必须匹配 `WJ15EDGK-3.81-02P-14-00A`，无法插接则不予测评。
- 方案和视频不能透露队伍、学校身份信息，否则总分扣 10 分。
- 比赛现场断网，现场会增加程序设计要求。

### 2.2 可以提前准备的通用能力

- ADC 通道抽象、校准、滤波和故障诊断框架。
- 参数描述表、命令路由表和 Modbus 寄存器映射框架。
- TF 卡循环日志、掉卡恢复和缓存刷新机制。
- FreeModbus 的 GD32 USART、定时器、临界区移植层。
- 自动化测试脚本、串口/Modbus 测试工具和离线资料库。
- PCB 输入保护、量程转换、模拟地、滤波和测试点模板。

### 2.3 正式题发布后必须重新确认

- 三路“相互隔离”是要求电气隔离、通道独立，还是仅要求互不干扰。
- “同时采集”是同步采样，还是允许 ADC 多路复用轮询。
- GD30AD3344 的数量是否限制为 1 片，还是允许使用多片样片。
- 4-20 mA 输入是有源电流源还是二线制变送器，采样板是否需要回路供电。
- 输入端允许的共模范围、过压范围、反接条件和浪涌等级。
- `+-1%` 是相对满量程、当前读数，还是各测试点独立误差。
- 断线的判定阈值、响应时间、恢复条件和上报格式。
- 串口私有报文、Modbus RTU 参数、从站地址和寄存器表。
- TF 文件名、格式、字段、采样周期、容量和掉电保存要求。
- 静态参数与定制参数具体包含哪些字段，以及保存介质。
- 是否继续考察初赛的 BootLoader、告警、睡眠、OLED 和设备 ID/波特率功能。

## 3. 硬件采样板任务分析

### 3.1 输入接口

现场信号源通过两位插拔端子连接，题图只给出“信号正”和“信号负”，未提供封装焊盘编号。

硬件设计时应执行以下规则：

- 采用与 `WJ15EDGK-3.81-02P-14-00A` 匹配的 3.81 mm 间距板端连接器。
- 根据实物插接方向确认 Pin 1/Pin 2，不能只按 PDF 图片左右位置编号。
- 原理图、丝印和测试文档统一标注 `SIG+`、`SIG-`。
- 每路增加正负极性、通道类型和编号标识，例如 `I1+ / I1-`、`V1+ / V1-`。
- 准备实物连接器做机械插拔验证，检查防呆方向、外壳干涉和接线空间。
- 在 PCB 上设置可测量的输入端、调理后电压、ADC 输入和地测试点。

### 3.2 4-20 mA 电流通道

典型方案是精密采样电阻将电流转换为电压：

```text
Iin -> 精密采样电阻 Rshunt -> 输入保护/RC 滤波 -> GD30AD3344
Vadc = Iin * Rshunt
Iin = Vadc / Rshunt
```

电阻选择需要给 ADC 满量程、输入误差和过流留余量。以 GD30AD3344 `+-2.048 V` 量程为例：

| Rshunt | 4 mA 电压 | 20 mA 电压 | 特点 |
|---:|---:|---:|---|
| 100 ohm | 0.4 V | 2.0 V | 分辨率高，但接近 2.048 V 上限，余量小 |
| 82 ohm | 0.328 V | 1.64 V | 余量较充分，适合加入保护和容差预算 |
| 50 ohm | 0.2 V | 1.0 V | 余量大，但有效码数降低 |

不能只按理想公式选择 100 ohm。正式电路还要考虑：

- 信号源可能超过 20 mA。
- 采样电阻温漂和自热。
- ADC 满量程、公差、增益误差与参考漂移。
- 输入保护器件漏电和钳位偏差。
- 单端/差分输入范围以及共模限制。

建议使用低温漂、`0.1%` 或更高精度的采样电阻，并至少做两点校准。若选 100 ohm，20 mA 时功耗为 40 mW，额定功率虽然不高，但精密误差预算仍应包含温升影响。

### 3.3 电流断线检测

对标准 4-20 mA 信号，断线通常表现为电流显著低于 4 mA。软件可设置迟滞阈值，例如：

```text
进入断线：I < I_break_low 且连续 N 次成立
恢复正常：I > I_break_recover 且连续 M 次成立
```

阈值不应在正式题公布前写死。备赛框架应让以下参数可配置：

- 断线进入阈值。
- 恢复阈值。
- 连续确认次数或确认时间。
- 故障码、Modbus 状态位和串口上报方式。

需要区分至少三种状态：`正常`、`欠量程/断线`、`过量程`。仅判断 ADC 等于 0 不可靠，因为噪声、偏置和有源信号源输出下限都可能产生非零值。

### 3.4 两路 0-10 V 电压通道

GD30AD3344 不能直接承受 10 V，需要电阻分压和保护。通用换算为：

```text
Vadc = Vin * Rbottom / (Rtop + Rbottom)
Vin  = Vadc * (Rtop + Rbottom) / Rbottom
```

若 ADC 使用 `+-2.048 V` 量程，则分压比应大于 `10 / 2.048 = 4.883`。例如约 5.1:1 的分压可使 10 V 映射到约 1.96 V，并保留少量余量。

工程设计还应考虑：

- 上下分压电阻使用低温漂、同系列精密器件，降低比值漂移。
- 输入阻抗不能给现场信号源造成过重负载。
- 高阻分压会增大噪声和 ADC 采样建立时间，可加入运放缓冲，但需校核输入/输出轨到轨范围。
- 输入端增加限流、RC、反接和过压保护，保护后的漏电不能破坏 `+-1%` 指标。
- 两路电压通道使用相同拓扑和器件批次，便于统一标定和备件管理。

### 3.5 误差预算

`+-1%` 不是单靠 ADC 位数就能保证。应建立误差预算表，至少包含：

| 误差来源 | 典型控制方式 |
|---|---|
| 采样/分压电阻初始精度 | 使用 0.1% 或更高精度电阻 |
| 电阻温漂和自热 | 低 TCR、合理功率余量 |
| ADC 偏置和增益误差 | 零点/满量程或两点校准 |
| ADC 噪声 | 合理数据率、数字滤波、多次平均 |
| 参考和电源漂移 | 稳定供电、去耦、参考校核 |
| PCB 漏电与地压降 | 模拟布线、单点回流、清洁度 |
| 保护和缓冲电路误差 | 低漏电保护、低失调运放 |
| 算法量化和系数误差 | 使用 float 或定点高精度运算 |

测试不应只测 4 mA、20 mA 或 0 V、10 V 两端点。建议至少测：

- 电流：4、8、12、16、20 mA，并增加断线、低于 4 mA、超过 20 mA。
- 电压：0、2、4、6、8、10 V，两路都测。
- 每个点记录标准源值、原始码、换算值、绝对误差和相对误差。
- 进行升序、降序各一轮，观察迟滞和漂移。
- 重启、长时间运行和不同供电方式下复测。

### 3.6 三路隔离与同时采集

这是样题中最需要等待正式解释的部分。

GD30AD3344 具有多路模拟输入 MUX，但单颗 ADC 通常是轮询转换，不等于三路严格同步采样。当前驱动支持 AIN0-AIN3 的多种单端/差分组合，可通过切换 MUX 轮询三路。

“三路信号相互隔离”可能有三种含义：

1. 三路具有独立调理网络，互不串扰。
2. 三路输入负端不能直接互连，需要差分采样或独立前端。
3. 三路要求真正的电气隔离，需要隔离电源、模拟隔离或数字隔离。

三者的成本、PCB 面积和方案完全不同。正式题未明确前，不应直接认定必须使用光耦/隔离电源，也不能默认共地一定合格。备赛时至少准备两套原理图策略：

- 低复杂度版本：三路独立保护、滤波、差分/单端调理，共用 ADC 和模拟地。
- 强隔离版本：按通道隔离供电/采样，或每路独立 ADC 后数字隔离。

如果正式题允许轮询采样，可采用通道状态机：切换 MUX、等待建立/转换、读取、滤波，再进入下一通道。对外发布数据时使用同一采样批次编号和时间戳，形成逻辑上的三通道快照。

### 3.7 PCB 评分风险

- 队伍编号必须放在**覆铜层**，不能只放丝印层。提交 Gerber 前应逐层检查。
- 队伍编号要求与文档匿名要求并不矛盾：PCB 按题目要求放队伍编号；方案和视频不得出现学校/队伍身份信息。具体展示范围以现场通知为准。
- 连接器封装必须用实物核验，不能仅依赖网络封装库。
- 模拟输入远离 SPI 时钟、SDIO、DC/DC 开关节点和 RS485 边沿信号。
- 为每个电源域配置去耦，为 ADC、运放、隔离电源保留调试测试点。
- PCB 设计完成后应进行 ERC、DRC、网络表、Gerber、钻孔和 3D 机械检查。
- 建议携带多块已焊接采样板，样题允许多块板调试和测评，但更换不补时。

## 4. 嵌入式软件任务分析

### 4.1 推荐软件分层

在现有初赛工程上扩展时，保持驱动、协议和业务分离：

```text
Driver/
  Components/gd30ad3344/   ADC SPI 传输与寄存器操作
  Components/sdio/         TF/SDIO 驱动
  Components/fatfs/        FatFs
  Components/bsp/          GD32 引脚、DMA、USART、定时器

Protocol/
  contest_protocol/        正式题下发的私有串口报文
  freemodbus/              FreeModbus 源码
  modbus_port/             GD32 串口、定时器、事件、临界区移植

Function/
  acquisition/             三通道采样状态机
  conversion/              电流/电压换算
  calibration/             校准参数与算法
  diagnostics/             断线、越限、ADC/TF 故障
  storage/                 TF 日志服务
  parameters/              静态/定制参数模型和持久化
  register_map/            Modbus 寄存器到业务数据的映射
```

不建议把正式题的新命令继续全部堆进当前单个 `proto_dispatch()`，也不建议让 Modbus 回调直接操作 ADC 或 FatFs。协议层只读写业务模型，采样和存储由独立任务推进。

### 4.2 三通道采集状态机

当前 `GD30AD3344_AD_Read()` 已能设置 MUX/PGA 并返回电压，但它面向单次阻塞读取。国赛更适合建立如下通用接口：

```c
typedef enum {
    AI_CURRENT_4_20MA,
    AI_VOLTAGE_0_10V_1,
    AI_VOLTAGE_0_10V_2
} ai_channel_t;

typedef struct {
    int16_t raw;
    float adc_volt;
    float engineering_value;
    uint32_t timestamp_ms;
    uint16_t status;
} ai_sample_t;
```

采集流程建议：

1. 配置当前通道 MUX 和 PGA。
2. 丢弃切换后的首个转换值，或按芯片手册等待建立时间。
3. 读取若干样本，执行中值/平均/IIR 滤波。
4. 应用通道校准参数 `value = raw * gain + offset`。
5. 执行断线、欠量程、过量程诊断。
6. 原子更新该通道最新快照。
7. 三路完成后形成一批数据，交给串口、Modbus 和 TF 存储使用。

正式题可能改变通道数量，因此通道描述应使用表驱动，而不是复制三套函数。

### 4.3 静态参数和定制参数

样题没有定义具体字段。建议预先建立统一参数模型：

- 设备参数：设备 ID、串口波特率、Modbus 地址、校验方式。
- 通道参数：启用状态、类型、量程、单位、采样周期、滤波参数。
- 校准参数：零点、增益、校准点、校准时间、校验码。
- 诊断参数：断线阈值、恢复阈值、确认次数。
- 存储参数：记录周期、文件滚动大小、启停状态。
- 定制参数：预留 TLV 或版本化扩展区，现场新增字段时不破坏旧结构。

参数结构应包含 `magic + schema_version + length + sequence + CRC`。初赛外部 Flash 当前使用单副本 byte-sum，国赛若考察可靠性，建议改成 A/B 双副本和 CRC32，并明确默认值、合法范围和迁移策略。

### 4.4 TF 卡存储

样题明确要求 TF 文件读写和多通道数据存储。现有项目已经具备 SDIO、FatFs、长文件名和基础读写演示，但 `sd_fatfs_test()` 只是启动测试，不是连续记录系统。

国赛需要新增日志服务，至少包含：

- 上电挂载和容量检查。
- 文件创建、追加、读取和关闭。
- CSV 或题目指定格式的三通道记录。
- 时间戳、批次号、三路工程值、通道状态和 CRC/校验字段。
- RAM 缓冲后批量写入，避免每个采样点都阻塞 FatFs。
- 定时 `f_sync()`，在性能和掉电丢失窗口之间权衡。
- 文件按日期、大小或记录数滚动。
- TF 不存在、写满、写保护、写入失败后的状态码和自动恢复。
- 通信查询日志时不能长时间阻塞采样。

建议的 CSV 仅作为自测格式，正式赛题若规定格式必须替换：

```text
timestamp_ms,current_mA,current_status,voltage1_V,voltage2_V,batch_id
12500,12.004,0,5.002,7.499,125
```

FatFs 当前 `_FS_REENTRANT=0`。现有工程是裸机调度，可以由唯一存储任务访问 FatFs；若后续引入 RTOS 或多个上下文同时访问，必须增加互斥或调整 FatFs 配置。

### 4.5 串口私有协议

样题只说“按照串口交互报文要求”，没有给出正式报文。现有初赛协议可复用以下基础：

- USART1 + RS485 DMA/IDLE 接收。
- RS485 DE 收发方向控制。
- ASCII 十六进制解析和组帧。
- CRC-16-Modbus 算法。
- 设备地址过滤、命令路由和错误应答。

不能提前假设国赛继续使用初赛的 `A5B6...B6A5` 帧，也不能把正式题协议强行塞进初赛命令字。建议保留 UART 字节流层，在上方提供可替换解析器：

```text
USART1 RX stream
  -> protocol detector / selected mode
     -> contest private protocol parser
     -> FreeModbus RTU parser
```

如果两种协议共用同一个 USART1，必须由正式题确认是否需要并存、切换或分时。Modbus RTU 以静默时间分帧，初赛 ASCII 协议以 IDLE/DMA 切包，两者不能未经设计就同时消费同一缓冲区。

### 4.6 FreeModbus 从站

当前工程只有 CRC-16-Modbus 算法，**没有 FreeModbus 协议栈**。两者不是一回事。FreeModbus 移植至少包括：

- `portserial.c`：USART 初始化、收发使能、字节中断回调。
- `porttimer.c`：Modbus RTU T3.5 定时器。
- `portevent.c`：协议栈事件队列。
- `portother.c`：临界区和断言等平台接口。
- `mb.c/mb_rtu.c/mbfunc*.c`：库本体和功能码处理。
- `eMBRegInputCB()`：输入寄存器读取。
- `eMBRegHoldingCB()`：保持寄存器读写。
- 必要时实现线圈、离散输入回调。

建议预备的寄存器模型如下，但地址以正式题为准：

| 数据 | 推荐寄存器类型 | 编码考虑 |
|---|---|---|
| 三路实时测量值 | Input Register | float 占 2 寄存器，提前准备字节序选项 |
| 原始 ADC 值 | Input Register | 有符号 16/32 位 |
| 通道状态/断线 | Input Register/Discrete Input | 位图或独立状态位 |
| 校准和滤波参数 | Holding Register | 写入后做范围校验和持久化 |
| 采样/记录启停 | Coil/Holding Register | 明确写入副作用 |
| 设备信息 | Input Register | 型号、版本、序列字段 |

FreeModbus 主循环通常需要持续调用 `eMBPoll()`。在当前裸机调度器中可以注册 1 ms 左右的 Modbus 任务，但 RTU 定时精度不能依赖一个可能被 TF 写入阻塞几十毫秒的普通轮询任务，T3.5 应由硬件定时器或可靠中断实现。

### 4.7 调度与实时性

当前工程的调度器是协作式轮询，任何同步 Flash/TF 写入、串口长帧发送或 ADC 阻塞等待都会推迟其他任务。国赛同时要求采样、存储和通信，建议至少做到：

- ADC 采集状态机短步骤执行，不在一次任务中长时间等待。
- USART RX 和 Modbus T3.5 使用中断/定时器保障。
- TF 写入采用 RAM 队列和分批刷新。
- 业务读取最新快照，不直接触发 ADC 阻塞采样。
- 设置每个任务的最坏执行时间和队列上限。
- 出现队列溢出时记录丢样计数，而不是静默覆盖。

是否切换 RTOS 要谨慎。样题没有要求 RTOS，现场临时开发时，熟悉且稳定的裸机事件框架通常比临时引入 RTOS 风险低。若队伍已经熟练掌握 RTOS，可以提前做好完整模板，但不能比赛现场才迁移架构。

## 5. 当前初赛工程可复用性评估

| 能力 | 当前状态 | 国赛样题适配判断 |
|---|---|---|
| GD32F470 BSP/标准库 | 已有 | 可直接作为主工程底座，符合 GD 库要求 |
| USART1 RS485 | 已有 DMA/IDLE 和 DE 控制 | 可复用底层，协议层需按正式题重做/扩展 |
| 初赛 ASCII 私有协议 | 已完整实现 | 可复用解析思想，不能假定正式题格式相同 |
| CRC-16-Modbus | 已有 | 可复用算法，但不等于 FreeModbus |
| GD30AD3344 SPI | 已有初始化、DMA 传输、MUX/PGA 读取 | 可复用驱动，需扩展三通道状态机、校准和诊断 |
| PT100 换算 | 已有单通道实现 | 算法参考价值有限，国赛改为电流/电压换算 |
| SDIO + FatFs | 已有挂载、文件读写和长文件名测试 | 底层可复用，缺连续日志、缓存、滚动和故障恢复 |
| 外部 Flash 参数 | 已有业务参数 blob | 可复用接口，可靠性和版本管理需加强 |
| RTC | 已有 | 可用于 TF 记录时间戳和文件名 |
| 裸机调度器 | 已有 | 可继续使用，但需解决 TF/ADC/Modbus 的阻塞和定时问题 |
| OLED/LED | 已有 | 可用于显示通道值、断线、TF、Modbus 状态 |
| BootLoader/OTA | 已有 | 样题未明确决赛是否考，建议保留但不要干扰现场开发 |
| FreeModbus | 未找到实现 | 当前最大明确软件缺口，需要提前离线集成和实测 |
| 三路工业模拟前端 PCB | 当前仓库无设计资料 | 当前最大硬件缺口，需要原理图、PCB、焊接和标定闭环 |

## 6. 推荐的国赛软件数据流

```text
GD30AD3344 + 三路模拟前端
  -> acquisition_task
  -> raw sample
  -> calibration + conversion + filter
  -> diagnostics
  -> atomic latest_snapshot
       |-> contest protocol response
       |-> FreeModbus register callbacks
       |-> OLED/LED status
       `-> log queue -> FatFs storage_task -> TF card

parameter service
  -> private protocol write
  -> Modbus holding-register write
  -> validation
  -> A/B persistent storage
  -> acquisition/storage configuration
```

该结构的关键是“采样一次，多处消费”。协议查询、Modbus 回调和 TF 存储都读取统一快照，避免三个模块各自触发 ADC，造成时序混乱和数据不一致。

## 7. 测试方案

### 7.1 硬件测试

- 上电前检查电源短路、输入端对地阻抗和连接器极性。
- 分电源域限流上电，检查 ADC、运放、隔离模块供电。
- 示波器检查 SPI 模式、时钟、CS 和数据建立保持关系。
- 输入 4-20 mA 和 0-10 V 全量程点，记录 ADC 原始码和调理节点电压。
- 单独改变某一路，确认另外两路变化在允许范围内，验证串扰。
- 反接、断线、欠量程、过量程测试应在保护设计允许范围内进行。
- 长时间运行观察温漂、供电噪声和 ADC 零点漂移。

### 7.2 软件功能测试

- 三路采样周期和批次一致性。
- 校准参数写入、重启恢复和非法参数拒绝。
- 断线进入、迟滞恢复和抖动边界。
- TF 插入、拔出、满卡、损坏文件系统和掉电重启。
- 私有协议正常帧、CRC 错、长度错、地址错和突发命令。
- Modbus 常用功能码、非法地址、非法值、广播写和超时。
- 私有协议和 Modbus 若共享串口，验证模式切换和粘包干扰。
- 采样、TF 连续写和高频通信同时运行的压力测试。

### 7.3 精度验收

每个测试点至少保存：

```text
通道、标准输入、原始 ADC、未校准值、校准后值、绝对误差、相对误差、状态、时间
```

误差计算口径应等待正式题确认。若题目未明确，可同时给出：

```text
读数相对误差 = (测量值 - 标准值) / 标准值 * 100%
满量程误差   = (测量值 - 标准值) / 满量程 * 100%
```

4 mA 附近用读数相对误差会更苛刻，不能只用满量程误差证明符合 `+-1%`。

## 8. 现场脱机开发准备

样题明确禁止现场联网，因此必须提前构建可离线使用的开发包：

- Keil、芯片 Pack、GD32 固件库和稳定工程模板。
- GD30AD3344 数据手册、参考原理图、寄存器速查表和现有驱动说明。
- FreeModbus 源码、许可证、移植说明、已验证 GD32 端口和寄存器示例。
- FatFs 文档、错误码表、SDIO 调试记录和可用 TF 卡。
- V1/V2 板原理图、引脚表、DMA 映射、USART/Timer/SDIO 资料。
- 串口助手、Modbus 主站工具、离线 Python/Node 测试脚本。
- Git 本地仓库、稳定标签、离线补丁和可回滚备份。
- 万用表、示波器、逻辑分析仪、USB-RS485、调试器、线材和备用电源。
- 多块控制板和采样板，以及已验证连接器和备用器件。

所有工具必须在断网状态下演练一次，确认许可证、驱动、Pack、字体和运行库不依赖在线下载。

## 9. 现场任务执行策略

1. 首先逐字读取正式题，建立“明确要求/待确认/禁止项”清单，不套用样题命令或地址。
2. 对照正式评分表，把任务拆成可独立验收的小项，优先打通供电、ADC、RS485、TF。
3. 从已验证的稳定 Git 标签创建比赛版本，不在唯一工作副本上大改。
4. 先建立统一三通道快照，再接私有协议、Modbus 和 TF，避免重复采样逻辑。
5. 每完成一个功能立即实物验证并提交本地 Git，记录可回退点。
6. 最后进行三路信号 + RS485 + TF 同时运行的连续压力测试。
7. 提交前重新编译、下载、断电冷启动，检查默认参数和正式报文。
8. 生成提交包后计算哈希并保留只读副本，确保提交代码与下装程序一致。
9. 程序交给边裁、作品封箱后不得再调整，封箱前执行最终检查表并由两名队员交叉确认。

## 10. 方案文档与匿名要求

最终进入答辩的队伍需要提交电子版和纸质版方案。样题要求方案至少覆盖：

- 系统功能描述及扩展。
- 功能设计与预期性能指标。
- 关键电路和核心代码解析。
- 测试环境、测试用例和可靠性测试。

建议方案结构：需求追踪矩阵、总体架构、硬件原理、误差预算、PCB 设计、软件架构、协议与寄存器、存储格式、故障诊断、测试结果、风险与改进。

匿名审查应单独执行：

- 文档正文、页眉页脚、图片、视频、文件属性和截图中不出现学校名称、缩写、校徽、建筑物等身份信息。
- PCB 是否展示队伍编号按题目要求执行，但不要附加学校标识。
- 检查 PDF 元数据、Office 作者字段、EDA 工程标题栏和 Git 提交作者是否会进入最终提交资料。

## 11. 备赛优先级

### P0：必须先完成

1. 完成并实测一块三路工业量采样板。
2. 建立 `+-1%` 精度测试和两点校准闭环。
3. 实现可靠的 4-20 mA 断线检测。
4. 将 FreeModbus 移植到 GD32F470 + USART1/RS485，并用主站软件连续测试。
5. 将现有 FatFs 演示改造成三通道连续日志服务。

### P1：形成现场快速适配能力

1. 表驱动通道、参数、私有命令和 Modbus 寄存器。
2. 支持可配置字节序、寄存器地址和采样周期。
3. 建立 ADC/TF/Modbus 状态码和 OLED 诊断页。
4. 准备离线自动测试工具和数据误差报告生成工具。
5. 完整演练一次断网限时开发和提交流程。

### P2：增强可靠性与答辩质量

1. 参数 A/B 双副本和 CRC32。
2. TF 日志滚动、掉卡恢复、掉电数据窗口说明。
3. 通道串扰、温漂、EMC 和长期运行测试。
4. 备份板一致性校准和快速换板流程。
5. 完整需求追踪矩阵和测试证据。

## 12. 最终判断

国赛样题表明，正式比赛很可能围绕“工业模拟量采集节点”展开，但现场会追加或改变程序要求。备赛的正确目标不是押中固定命令字，而是建立一个能够快速配置的稳定平台：

- 硬件上，三路输入安全、精确、可校准、可诊断、连接可靠。
- 软件上，采样、参数、TF、私有协议和 Modbus 通过统一业务模型解耦。
- 工程上，所有工具和资料可离线使用，代码可回滚，测试有证据。
- 文档上，能解释误差预算、关键电路、数据流和可靠性设计，同时满足匿名要求。

当前初赛代码提供了较好的 GD32 BSP、RS485、GD30AD3344、FatFs、RTC 和参数基础，但距离样题要求仍有三个主要缺口：**三路工业模拟前端 PCB、面向连续采集的 TF 日志服务、完整 FreeModbus 从站移植**。这三项应作为全国总决赛备赛的主线，正式题发布后再替换具体报文、寄存器和参数定义。
